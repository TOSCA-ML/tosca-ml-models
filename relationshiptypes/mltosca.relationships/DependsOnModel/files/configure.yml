---
- name: Write models to file
  hosts: localhost

  tasks:
    - name: Create my_folder directory
      file:
        path: "{{ project_location }}/evaluation"
        state: directory

    - name: Check if file exists
      stat:
        path: "{{ project_location }}/evaluation/models.txt"
      register: file_info

    - name: Append input to file
      copy:
        dest: "{{ project_location }}/evaluation/models.txt"
        content: |
          {% if file_info.stat.exists %}
          {{ lookup('file', '{{ project_location }}/evaluation/models.txt') }}
          {% endif %}
          {{ function_id }}#_#{{ data_folder }}
